"use client";

import React, { useState } from "react";

export default function AddPriority({ togglePriority }: any) {
    const [priority, setPriority] = useState("");

    const [windowWidth, setWindowWidth] = useState(window.innerWidth);

    return (
        <div>

        </div>
    );
}